# Auth Service Configuration

# Server Configuration
PORT=3001                                    # Port on which the Auth Service will listen

# JWT Configuration
JWT_SECRET=your-secret-key-here              # Secret key for signing JWT tokens (MUST match api-gateway)
JWT_EXPIRATION=24h                           # JWT token expiration time (e.g., 24h, 7d, 30m)

# Database Configuration
DB_HOST=localhost                            # PostgreSQL database host
DB_PORT=5432                                 # PostgreSQL database port
DB_NAME=ai_evaluation_platform               # PostgreSQL database name
DB_USER=postgres                             # PostgreSQL database user
DB_PASSWORD=postgres                         # PostgreSQL database password
